<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokemon</title>
    <style>
    div {
        margin: 0 auto;
        text-align: center;
    }
    </style>
</head>
<body>
    <div> 
        <a href="/">Go Back</a>
        <p>ID: <%= pokemon.id %></p>
        <p>Name: <%= pokemon.name %></p>
        <p>Height: <%= pokemon.height %></p>
        <img height="200px" width="auto" src="<%= pokemon.sprites.front_default %>" />
        <form action="/savePokemon" method="POST">
            <input type="hidden" value="<%= pokemon.id %>" name="pokemonId" /> 
            <input type="hidden" value="<%= pokemon.name %>" name="pokemonName" />
            <input type="hidden" value="<%= pokemon.height %>" name="pokemonHeight"/>
            <input type="hidden" value="<%= pokemon.sprites.front_default %>" name="pokemonPhoto" />
            <input type="submit" value="Add to my Saved Collection" />
        </form>
    </div>
</body>
</html>